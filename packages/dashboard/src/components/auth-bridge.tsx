"use client";

/**
 * Auth bridge — legacy placeholder.
 * Session auth uses localStorage tokens directly via api.ts.
 * This component is kept for backward compatibility but does nothing.
 */
export function AuthBridge() {
  return null;
}
